/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "a9yikj8ndyfzbdsz.public.blob.vercel-storage.com",
      },
    ],
  },
};

export default nextConfig;
